<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cilicili.mapper.UserMapper">
    <select id="selectByUsername" resultType="com.cilicili.entity.User">
        SELECT id, username, password, nickname, avatar
        FROM user
        WHERE username = #{username}
    </select>

    <select id="selectByUserPhone" resultType="com.cilicili.entity.User">
        SELECT id, username, password, nickname, avatar
        FROM user
        WHERE phone = #{userPhone}
    </select>

    <!-- 增加硬币 -->
    <update id="addCoin">
        UPDATE user
        SET coin = coin + #{amount}
        WHERE id = #{userId} AND #{amount} > 0
    </update>

    <!-- 减少硬币 -->
    <update id="subtractCoin">
        UPDATE user
        SET coin = coin - #{amount}
        WHERE id = #{userId} AND coin >= #{amount}
    </update>
</mapper>