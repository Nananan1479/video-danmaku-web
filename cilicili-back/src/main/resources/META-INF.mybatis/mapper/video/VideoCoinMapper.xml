<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cilicili.mapper.interaction.VideoCoinMapper">

    <!-- 检查用户是否已投币 -->
    <select id="existsByVideoIdAndUserId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM video_coin
        WHERE video_id = #{videoId} AND user_id = #{userId}
    </select>

    <!-- 获取视频投币总数（num 列求和） -->
    <select id="sumCoinByVideoId" resultType="int">
        SELECT COALESCE(SUM(num), 0)
        FROM video_coin
        WHERE video_id = #{videoId}
    </select>

    <!-- 用户投币 -->
    <insert id="insertCoin">
        INSERT INTO video_coin (video_id, user_id, num, created_at)
        VALUES (#{videoId}, #{userId}, #{num}, NOW())
    </insert>

    <!-- 用户取消投币 -->
    <delete id="deleteByVideoIdAndUserId">
        DELETE FROM video_coin
        WHERE video_id = #{videoId} AND user_id = #{userId}
    </delete>

    <!-- 获取用户投币的视频ID列表 -->
    <select id="selectCoinedVideoIds" resultType="long">
        SELECT video_id
        FROM video_coin
        WHERE user_id = #{userId}
    </select>

</mapper>
