<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cilicili.mapper.interaction.VideoLikeMapper">

    <!-- 检查用户是否已点赞 -->
    <select id="existsByVideoIdAndUserId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM video_like
        WHERE video_id = #{videoId} AND user_id = #{userId}
    </select>

    <!-- 获取视频点赞数 -->
    <select id="countByVideoId" resultType="long">
        SELECT COUNT(*)
        FROM video_like
        WHERE video_id = #{videoId}
    </select>

    <!-- 用户点赞 -->
    <insert id="insertLike">
        INSERT INTO video_like (video_id, user_id, created_at)
        VALUES (#{videoId}, #{userId}, NOW())
    </insert>

    <!-- 用户取消点赞 -->
    <delete id="deleteByVideoIdAndUserId">
        DELETE FROM video_like
        WHERE video_id = #{videoId} AND user_id = #{userId}
    </delete>

    <!-- 获取用户点赞的视频ID列表 -->
    <select id="selectLikedVideoIds" resultType="long">
        SELECT video_id
        FROM video_like
        WHERE user_id = #{userId}
    </select>

</mapper>
